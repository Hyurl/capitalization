/** Capitalizes every word in a string. */
export declare function capitalize(text: string, noHyphen?: boolean): string;

/** Hyphenates a capitalized string. */
export declare function hyphenate(text: string, lowerCase?: boolean): string;

/** Transfers a string with underlines to camel case. */
export declare function camelCase(text: string, ucfirst?: boolean): string;

/** Transfers camel-cased string to underline. */
export declare function underline(text: string, keepCase?: boolean): string;